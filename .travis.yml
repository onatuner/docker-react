language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t onatuner/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true onatuner/docker-react npm run test
